(function () {

  var offset = Math.floor(window.innerHeight / 4);

  new WOW({
    offset: offset,
    mobile: false
  }).init();


  // текущая ширина контейнера
  var containerWidth = 1000;
  // autowidth

})();
